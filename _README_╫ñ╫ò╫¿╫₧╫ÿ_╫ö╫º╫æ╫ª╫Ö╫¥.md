# קוד המקור — פורמט הקבצים

הקבצים כאן הם פלט **Claude Design** (`שם המסך.dc.html`). הם מקור האמת למידות, צבעים, טקסטים ומבנה.

## מבנה קובץ
- עוטף `<x-dc>`, בלוק `<helmet>` (פונט Heebo + סגנונות גלובליים), ואז התוכן כ-**HTML עם inline-styles**.
- בסוף הקובץ: `<script type="text/x-dc">` עם `class Component extends DCLogic { renderVals() {…} }` — מכיל את **הנתונים והלוגיקה** של החלקים הדינמיים.
- חלקים דינמיים בתבנית: `<sc-for list="{{ … }}">` (לולאה) ו-`<sc-if value="{{ … }}">` (תנאי), עם `{{ … }}` כ-binding.

## איך לקרוא / להריץ
- **לקריאת ערכים מדויקים** (צבע/מידה/טקסט/SVG): פתחו את הקובץ בעורך — רוב התוכן סטטי וקריא ישירות.
- **לתצוגה מרונדרת** (כולל הרשימות הדינמיות): הרפרנס הוויזואלי המדויק הוא **צילומי המסך בגובה מלא** שבחבילה. פתיחה ישירה של `.dc.html` בדפדפן דורשת את ה-runtime (`support.js`) **יחד עם React/ReactDOM** הנטענים לפניו; בלעדיהם הרשימות הדינמיות לא יוצגו. לכן לעבודה שוטפת — עבדו מול צילום המסך + קוד המקור.
- אין תלות בקובצי תמונה חיצוניים; כל האייקונים הם SVG inline.

> ביישום שלכם תחליפו את `<sc-for>`/`<sc-if>` בלולאות/תנאים של ה-framework, ותעבירו את הנתונים מה-`<script>` למודל הנתונים שלכם.
